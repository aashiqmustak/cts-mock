version: '3.8'

services:
  api:
    build: .
    container_name: prior_auth_api
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
      - ./models:/app/models
    environment:
      - ENV=production
    restart: unless-stopped
